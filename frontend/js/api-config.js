/* ============================================================
   api-config.js
   ------------------------------------------------------------
   Set this to your deployed Render backend URL after deployment.
   Until then it defaults to a local dev URL, and if the API is
   unreachable the site simply falls back to the original
   catalog-data.js content — nothing breaks.
   ============================================================ */
window.MU_API_BASE = 'https://maison-utsav-backend.onrender.com';
// For local testing against `npm run dev` in /backend, use:
// window.MU_API_BASE = 'http://localhost:4000';
window.MU_WHATSAPP_NUMBER = '919575929021'; // e.g. '919575929021'
